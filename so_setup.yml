
#agregar a sudo visudo al final
#soniontest ALL=(ALL) ALL


---

- name: Install Security Onion
  hosts: all
#  become: yes
  gather_facts: yes

  vars_prompt:
  - name: 'so_ssh_master_password'
    prompt: 'SSH password for Security Onion Master'
    private: yes
    when: "'forward_nodes' in group_names"

  roles:
    - securityonion_setup

  
